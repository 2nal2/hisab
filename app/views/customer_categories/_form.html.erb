<%= form_with(model: customer_category, local: true) do |form| %>
  <% if customer_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer_category.errors.count, "error") %> prohibited this customer_category from being saved:</h2>

      <ul>
      <% customer_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_name %>
    <%= form.text_field :category_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
