<%= form_with(model: company_department, local: true) do |form| %>
  <% if company_department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_department.errors.count, "error") %> prohibited this company_department from being saved:</h2>

      <ul>
      <% company_department.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
